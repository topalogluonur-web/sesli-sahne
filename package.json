{
  "name": "sesli-sahne-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../admin && npm install && cd ../mobile && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev:mobile": "cd mobile && npm run start"
  }
}
